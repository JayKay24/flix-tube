apiVersion: v1
kind: Service
metadata:
  name: video-storage
spec:
  selector:
    app: video-storage
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4001
      nodePort: 30001