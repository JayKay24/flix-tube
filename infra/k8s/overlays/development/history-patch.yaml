apiVersion: v1
kind: Deployment
metadata:
  name: history
spec:
  selector:
    matchLabels:
      app: history
  template:
    spec:
      containers:
        - name: history
          resources:
            limits:
              memory: "128Mi"
            requests:
              memory: "128Mi"
              cpu: "100m"