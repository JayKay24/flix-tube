apiVersion: apps/v1
kind: Deployment
metadata:
  name: video-upload
spec:
  selector:
    matchLabels:
      app: video-upload
  template:
    spec:
      containers:
        - name: video-upload
          image: $CONTAINER_REGISTRY/staging-video-upload:$VERSION
          imagePullPolicy: IfNotPresent
          resources:
            limits:
              memory: "128Mi"
            requests:
              memory: "128Mi"
              cpu: "100m"
