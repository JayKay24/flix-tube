include:
  - ./docker-compose-video-storage.yml
services:
  azure-storage:
    build:
      dockerfile: apps/mock-storage/Dockerfile-dev
    volumes:
      - ./:/app:z
      - /app/node_modules
      - /app/.nx
